0.1.37
- Removed the README section for installing from the ZIP bundle, as requested.
- Kept the direct GitHub installation section as the primary install method.

Sahar 0.1.36

- Merged the runtime bot fixes that were validated on the live server into the project source.
- Guarded optional Telegram bot handler registration so missing commands no longer crash startup.
- Kept warning logs for skipped optional handlers instead of raising NameError.
- Synchronized installer version strings, user-agent strings, README examples, and generated package_version to 0.1.36.
- Updated zip/install examples in README to the current single-installer package name.
