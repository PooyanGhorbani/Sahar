Sahar 0.1.26

- Master installer now validates BOT_TOKEN automatically against the Telegram getMe API before continuing.
- Installer stops early with a clear error if the bot token is invalid or Telegram API cannot be reached.
- Version strings were synchronized across the bundle.
